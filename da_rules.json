{
  "rules": {
    "user-scores": {
      ".indexOn": "timestamp",
      "$scores": {
     	 	".read": "true",
	  	  ".write": "!root.child('user-scores/'+$scores).exists()"
      }
    },
    "questions": {
      ".read": "true",
    	".write": "false"

    }
  }
}
